;;
;; Signal
;;

syntax xsignal =
  | REJECT val
  | EXIT

syntax signal(syntax V) =
  | xsignal
  | CONT V

syntax ssignal =
  | CONT
  | RET val?
  | ACCEPT
  | REJECT val
  | STATE state_label
  | EXIT
